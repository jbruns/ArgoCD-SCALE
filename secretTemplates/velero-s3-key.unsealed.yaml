apiVersion: v1
kind: Secret
metadata:
  name: velero-s3-key
  namespace: argo-common
data:
  #    [default]
  #    aws_access_key_id=<REDACTED>
  #    aws_secret_access_key=<REDACTED>
  cloud: |-
    <base64 encoded creds>